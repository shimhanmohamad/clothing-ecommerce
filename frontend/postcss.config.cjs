module.exports = {
  plugins: {
    // Tailwind's PostCSS plugin has moved to a separate package.
    // Use @tailwindcss/postcss here as instructed by the error message.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
